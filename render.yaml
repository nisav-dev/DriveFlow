services:
  - type: web
    name: driveflow-app
    runtime: docker
    plan: free
    rootDir: .
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: SPRING_H2_CONSOLE_ENABLED
        value: "false"
